{
    "name": "your-project-name",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "preview": "vite preview",
        "deploy": "gh-pages -d dist"
    },
    "dependencies": {
        "three": "^0.165.0"
    },
    "devDependencies": {
        "gh-pages": "^6.1.1",
        "vite": "^5.3.2"
    }
}
